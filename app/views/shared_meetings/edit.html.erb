<!-- Validation failure messages -->
<% if @shared_meeting.errors.any? %>
  <% @shared_meeting.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Edit Shared Meeting #<%= @shared_meeting.id %></h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/update_shared_meeting/<%= @shared_meeting.id %>" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      
      

      <button class="btn btn-warning">
        Update Shared Meeting
      </button>
      or
      <a href="/shared_meetings/<%= @shared_meeting.id %>">Cancel</a>
    </form>
  </div>
</div>
